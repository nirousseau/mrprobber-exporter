---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mrprober
  name: mrprober
  namespace: monitoring
spec:
  ports:
    - name: http
      port: 9100
      protocol: TCP
      targetPort: 9100
  selector:
    app: mrprober
  type: ClusterIP
