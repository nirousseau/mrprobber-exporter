# TODO

* Priority : Write tests ! (code cov >= 80%)
* Add max budget Deadline (5sec ?) for each tick in Deamon mode.
* Add graphs from timeout when using net probes
* Add assertion section for manipulating the return code (for example check that the connectivity is failed on purpose)
